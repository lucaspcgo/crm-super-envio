# config/

Configuração central do app. Mude aqui pra customizar identidade.

- **`app.config.ts`** — nome do app, descrição, branding, feature flags
- **`nav.config.ts`** — itens da sidebar com filtro por role

Edite esses arquivos antes de qualquer customização visual. Todo o resto do código consome daqui.
